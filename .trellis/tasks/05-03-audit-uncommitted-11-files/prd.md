# PRD - 审计当前未提交改动（11 文件）

## 目标
对 `D:\miaowu-os\deer-flow-main` 当前工作区未提交改动（11 个已修改文件）执行静态审计与最小必要验证，输出按严重性排序的问题清单。

## 范围
- 仅审计 `git status` 中当前已修改的 11 个文件。
- 不提交代码，不做无关重构。

## 验证要求
- 运行最小必要验证：`ruff`、目标 `pytest`、必要 `py_compile`。
- 若验证无法完成，必须报告缺口。

## 输出要求
1. 严重性排序的问题清单（含文件与行号）
2. 若无问题明确写“未发现阻断问题”
3. 剩余风险
