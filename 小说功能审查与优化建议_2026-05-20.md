# 小说功能审查与优化建议（2026-05-20）

> 审查范围：`D:\miaowu-os\deer-flow-main` 二开新增的"小说创作"模块（`novel_migrated` + `routers/novel` + 前端 `app/workspace/novel` + `components/novel` + `core/novel` + harness 中的 `novel_*_tools`）。
> 审查方法：4 个并行代理交叉审查路由层 / 服务与 AI 工具层 / 前端 core 数据层 / 前端页面与组件层，再由主代理汇总归并。
> 项目定位：本地单用户场景（`local_single_user` 默认回退），但代码同时承担"未来多用户化"准备工作；安全/鉴权类问题在本地场景降级，但作为多用户化阻塞项保留在路线图中。
> 历史参考：`CODE_REVIEW_REPORT.md`（2026-04-24）、`Novel_Creation_Process_Assessment_Report.md`、`novel-tool-gap-analysis.md`、`小说提示词模板系统排查报告.md`。

---

## 一、执行摘要

### 1.1 体量盘点

| 区域 | 文件数 | 总行数 | 备注 |
|------|--------|--------|------|
| 后端 API 端点 | 27 个 api 模块 + 老 router | **263 个** REST 端点 | `novel_migrated/api` 201 + `routers/novel` 62 |
| 后端业务 service | 33 个 .py | **17 329 行** | 单文件最大 2 725 行（book_import） |
| 后端 ORM model | 25 个 | — | SQLite + WAL，单库 30 表 |
| 后端 AI 工具 | 8 个 builtin | **3 749 行** | 14 个 LangChain @tool |
| 前端页面 | 22 个 .tsx | 653 行 | App Router 14 个动态子路由 |
| 前端组件 | 30+ 个 .tsx + 15 子目录 | **11 457 行** | 单文件最大 1 347 行（AIProjectGenerator） |
| 前端 core 数据层 | 19 个 .ts(x) | **6 261 行** | 单文件最大 1 793 行（novel-api.ts） |
| 提示词模板 | YAML 1207 行 | 37 个模板 | 已支持外置 + 热加载 |

### 1.2 总体结论

小说功能完成度已经达到"具备完整创作链路（灵感 → 世界观 → 角色 → 大纲 → 章节 → 分析 → 润色 → 拆书导入）"的程度，关键工程能力（流式 SSE、提示词外置、文件真相桥、幂等键、乐观锁、工作区文档同步）均已搭建。但同时存在六类系统性问题，需要在"完成度"之外补一次"工程质量收敛"：

1. **双轨架构未收口**：`routers/novel.py`（in-memory + JSON 文件）与 `routers/novel_migrated.py`（SQLAlchemy + 30 张表）并行注册，端点重叠且部分老路由仍被中间件依赖。
2. **harness 反向依赖 app 层**：`packages/harness/deerflow/tools/builtins/novel_internal.py` 通过 `importlib` 反向加载 `app.gateway.novel_migrated.*`，违反 `tests/test_harness_boundary.py` 强制约束的"harness 不依赖 app"边界。
3. **AI 事件总线监听器泄漏 + 并发串扰**：`useAiPanelStore.ts` 在模块顶层注册 4 个 `aiEventBus.on(...)`，永不解绑；`aiStream` 是单例，无法承载多并发流（章节生成 + 聊天 + 大纲同时跑会互相覆盖）。
4. **巨型文件聚集**：后端 `book_import_service.py`（2 725 行）、`foreshadow_service.py`（1 699 行）、`ai_service.py`（1 119 行）；前端 `novel-api.ts`（1 793 行）、`AIProjectGenerator.tsx`（1 347 行）、`RelationshipGraph.tsx`（1 246 行）、`ChapterAnalysis.tsx`（1 032 行）。
5. **四级缓存与双写**：服务器数据同时活在 React Query / `useNovelStore` / `_novelCache`（30 s LRU）/ Dexie 四处，`sync-hooks.ts` 把 React Query 数据回写到 zustand，写操作只 `invalidateQueries` 不清 `_novelCache`，存在最长 30 秒的数据陈旧窗口。
6. **SSE 与 AI 调用样板复制 5+ 份**：前端 `AIProjectGenerator` / `ChapterRegenerationModal` / `InspirationMode` / `Editor` / `GeneratePanel` / `BookImportPage` 各自实现 SSE 解析；后端 18 个 LangChain 工具复制 25 行"check_idempotency → try internal → fallback HTTP → attach_file_truth_meta"模板。

### 1.3 Top 5 风险（按本地单用户场景修正排序）

1. **AI 事件总线监听器泄漏 + 多并发流互相覆盖** — 长期使用必然内存泄漏 + UI 数据串流。
2. **流式路径 `tool_calls` 被静默吞掉** — `ai_service.py` 的 `generate_text_stream*` 没处理 `chunk.tool_calls`，模型选择调工具时调用方拿到空字符串。
3. **harness → app 反向依赖** — 一旦运行 boundary 测试覆盖 builtins 立即红灯；harness 单独发布会崩。
4. **datetime 时区混用** — `utcnow()` / `now()` / `now(tz=UTC)` 三种写法混用，迁移到 Postgres 必触发 `TypeError`。
5. **Editor 章节切换可能写错章节** — `previousChapterRef` 缺依赖数组 + 切章 effect 缺依赖，存在脏内容写到错章节的窗口期。

---

## 二、整体架构与亮点

### 2.1 现有架构

```
┌────────────────────────────────────────────────────────────────────────┐
│  前端  Next.js 16 + React 19                                            │
│  app/workspace/novel/[novelId]/{chapters,characters,outline,...}       │
│      ↓ 路由薄壳                                                         │
│  components/novel/{Editor,AIProjectGenerator,RelationshipGraph,...}    │
│      ↓ React Query / zustand                                            │
│  core/novel/{novel-api(1793),queries(732),database(730 Dexie),...}     │
│      ↓ authFetch + SSE                                                  │
└────────────────────────────────────────────────────────────────────────┘
                                ↓ HTTP
┌────────────────────────────────────────────────────────────────────────┐
│  后端  FastAPI Gateway （8551，本地）                                   │
│  routers/novel.py        ← 老路由（in-memory + novel_store.json）       │
│  routers/novel_migrated  ← 聚合 27 个 api 模块（SQLAlchemy + WAL）      │
│      ↓                                                                  │
│  novel_migrated/services/  33 个 service                                │
│      ↓                                                                  │
│  .deer-flow/novel_migrated.db  + .deer-flow/novel-workspaces/{user}/   │
└────────────────────────────────────────────────────────────────────────┘
                                ↓ 进程内 / HTTP
┌────────────────────────────────────────────────────────────────────────┐
│  AI 工具层  packages/harness/deerflow/tools/builtins/novel_*           │
│  novel_tools.py      ← 老工具（HTTP 自调用）                            │
│  novel_creation_tools.py / novel_analysis_tools.py / ...                │
│       通过 novel_internal.py importlib 直调 app 层（边界违规）          │
│  novel_idempotency.py + novel_file_truth_bridge.py                      │
└────────────────────────────────────────────────────────────────────────┘
```

### 2.2 已识别的工程亮点

1. **提示词外置 + 热加载**：`novel_migrated/resources/prompt_templates.yaml`（1 207 行 / 37 模板）+ `prompt_service._ensure_system_templates_loaded()` 监 mtime 自动 reload。`PromptService.get_template()` 优先读 DB 中用户自定义模板，30 s TTL 缓存。
2. **文件真相桥**：`novel_file_truth_bridge.py` 把工具产出的大正文写入 `.deer-flow/novel-workspaces/{user}/{project}/{entity}` 文件并同步到 `document_index` 表，工具响应只回流元数据（`doc_path / content_hash / size`），有效防止 LangChain 上下文爆炸。
3. **幂等键 + 异步落盘**：`novel_idempotency.py` 用 `threading.Lock + queue + 后台 daemon` 异步持久化幂等键，主路径无阻塞。
4. **进程内直调路径**：`novel_internal.get_internal_ai_service()` 让 `novel_creation_tools / novel_analysis_tools / novel_extended_tools` 在 harness 内不走 HTTP 直接拿到 AI service 实例（虽然边界有问题，但思路正确）。
5. **加密设置**：`novel_migrated/core/crypto.py` 用 Fernet 对 API key 等敏感字段做对称加密，并通过 `safe_decrypt` 兼容历史明文。
6. **SQLite WAL + Pragma 优化**：`database.py` 启用 WAL、`synchronous=NORMAL`、`cache_size=-64000`、`foreign_keys=ON`，已经为高频写入做了基础调优。
7. **路由 fallback 兼容**：`novel_tool_helpers._build_route_fallback_url` 在 `/api/...` 与裸前缀之间自动 fallback，便于不同部署环境。
8. **统一 `_ok / _fail` 返回结构**：所有 novel 工具返回结构一致，便于 LangChain 端解析。

这些是"二开沉淀的工程资产"，本次优化不是要推翻它们，而是要把它们的价值最大化（例如把所有提示词都迁入 YAML、所有工具都用统一装饰器、所有 SSE 都用统一抽象）。

---

## 三、严重问题清单

> 严重度定义：**严重** = 数据丢失 / 内存泄漏 / 数据错乱 / 已有 boundary 测试会炸 / 调用方静默失败；**高** = 性能瓶颈、明显代码重复、错误处理缺失、UI 阻塞；**中** = 维护性、状态管理混乱；**低** = 命名、样式、注释。本节只列**严重**与**高**。

### 3.1 后端严重问题

| 编号 | 严重度 | 问题 | 位置 |
|------|--------|------|------|
| B1 | 严重 | harness 通过 `importlib.import_module("app.gateway.novel_migrated...")` 反向依赖 app 层，违反 `tests/test_harness_boundary.py` 强制约束 | `packages/harness/deerflow/tools/builtins/novel_internal.py:25-39` |
| B2 | 严重 | 流式路径 `generate_text_stream` / `generate_text_stream_with_messages` 没处理 `chunk.tool_calls`，模型选择调用工具时 content 段为空，调用方拿到空字符串、不知错 | `app/gateway/novel_migrated/services/ai_service.py:826-936` |
| B3 | 严重 | 6 个创作工具 internal 调用失败后无差别 fallback HTTP，`IntegrityError` / `CancelledError` 也会再发一次 HTTP，可能造成同条请求落库两次 | `packages/harness/deerflow/tools/builtins/novel_creation_tools.py:384-836` |
| B4 | 严重 | `datetime.utcnow()` / `datetime.now()` / `datetime.now(tz=UTC)` 三种写法混用，迁 Postgres 必触发 `TypeError: can't compare offset-naive and offset-aware datetimes` | `recovery_service.py:26`、`orchestration_service.py:413,455,485,498,564,666,729,762`、`cover_generation_service.py:116,317`、`foreshadow_service.py:1450,1533,1483,1547` |
| B5 | 严重 | 幂等键单键单文件落盘，`_MAX_DISK_FILES=10000` 超限直接放弃；以 `Path.cwd()` 为基准而非项目根，多 worker 不一致 | `novel_idempotency.py:185-210` |
| B6 | 严重 | `routers/novel.py` 的 NovelStore 全套 `/api/novels/*` 端点完全无鉴权依赖且 `await request.json()` 直收 dict，越权可写 + 字段注入；`get_novel/delete_novel` 还会 fallback 到 modern store 删除其他用户数据。本地单用户场景下不算严重，但作为多用户化阻塞项 | `backend/app/gateway/routers/novel.py:921-1196` |
| B7 | 严重 | `memories.py` / `careers.py` / `chapters.py` / `novel_stream.py` 把 `user_id: str | None = None` 暴露成查询参数并 `effective_user_id = user_id or get_user_id(request)`，附 `?user_id=xxx` 即可绕过 token | `memories.py:95,547`、`careers.py:281`、`chapters.py:510`、`novel_stream.py:1301` |
| B8 | 严重 | 中间件仍依赖 deprecated 的 `_novel_store`：`intent_recognition_middleware.py` 通过 `from app.gateway.routers.novel import _novel_store` 写入 legacy store，形成"双写"反模式 | `app/gateway/middleware/intent_recognition_middleware.py:4074-4133` |
| B9 | 高 | AI 模型缓存 key 含 `(thread_id, loop_id)`，多 worker / `asyncio.to_thread` 混合环境命中率低；缓存满后频繁创建实例，无命中率告警 | `services/ai_service.py:103-261` |
| B10 | 高 | 模型调用层无超时与指数退避；`call_with_json_retry` 只重试 JSON 解析，模型本身的 5xx / RateLimit / 网络异常没有 retry，整条 `book_import` 链路出错时一次性失败 | `services/ai_service.py:562-625, 1012` |
| B11 | 高 | 全局 `httpx.AsyncClient` 无连接池上限、未设 `transport limits`，`is_closed` 时只新建不关闭 | `packages/harness/deerflow/tools/builtins/novel_tool_helpers.py:13-20` |
| B12 | 高 | SSE 错误事件把 `str(exc)` 直接回传客户端，泄漏 SQLAlchemy / AI 服务内部消息 | `novel_stream.py:273-275, 1210-1222`、`wizard_stream.py:117-118` |
| B13 | 高 | `routers/novel.py` 的 11 个 POST/PUT 端点全部用 `await request.json()` 直收 dict，无 Pydantic 校验、无字段白名单、无大小限制 | `routers/novel.py:976-1196` |
| B14 | 高 | `novel_creation_tools.py:341` 直接调用 `ai_service._clean_json_response(...)`（私有方法），跨包跨边界引用私有 API；公共 API 是 `clean_json_response` | `novel_creation_tools.py:341` |
| B15 | 高 | `_STREAM_REQUEST_WINDOWS` / `_ANALYSIS_TASKS` / `_ANALYSIS_RESULTS` 全是模块级 dict/deque，无锁，多 worker 下限流被乘以 worker 数 | `novel_stream.py:42` |
| B16 | 高 | 大量 `except Exception as e: raise HTTPException(500, detail=f"...{str(e)}")` 把内部异常字符串回写客户端 | `admin.py:181-258`、`changelog.py:138`、`foreshadows.py:28-30`、`memories.py` 多处 |
| B17 | 高 | `SSEResponse` 在 SSE 头硬编码 `Access-Control-Allow-Origin: *`，与 Gateway 全局 CORS（`GATEWAY_CORS_ORIGINS`）冲突 | `novel_migrated/utils/sse_response.py:418-428` |
| B18 | 高 | 路由前缀风格混乱：`admin/chapters/characters/outlines/...` 用裸前缀，`careers/foreshadows/inspiration/memories/...` 又自带 `/api`，最终 URL 取决于 app.py 是否再加前缀，前端易拼错 | `novel_migrated/api/*.py` |

### 3.2 前端严重问题

| 编号 | 严重度 | 问题 | 位置 |
|------|--------|------|------|
| F1 | 严重 | `aiEventBus.on(...)` × 4 在模块顶层注册且永不解绑；React 19 + Next 16 双重 mount/HMR/RSC 重导入会反复入栈，listener Set 持续膨胀 | `frontend/src/core/novel/useAiPanelStore.ts:77-99` |
| F2 | 严重 | AI 事件不按 `requestId` 派发，`useAiPanelStore` 只持单一 `latestChunk/seq`；多并发流（章节生成 + chat + 大纲）会互相覆盖 | `core/novel/ai-event-bus.ts:13-71` + `useAiPanelStore.ts` |
| F3 | 严重 | `sync-hooks.ts` 双写：`useProjectSync` 把整本 novel 同步到 store，`useChapterSync/useCharacterSync` 又把 list 覆盖；同页同时挂载 → 写入顺序不确定，最后写胜者不稳定 | `core/novel/sync-hooks.ts:24-221` |
| F4 | 严重 | `useOutlineStore.moveNode` 用 `JSON.parse(JSON.stringify(state.tree))` 深克隆整树；Date / Function / undefined 字段丢失，大纲较大时阻塞主线程 | `core/novel/useOutlineStore.ts:143` |
| F5 | 严重 | `_novelCache` 与 React Query 缓存无失效联动：写操作只 `invalidateQueries`，不清 `_novelCache`，30 s 内 `getCharacters/getChapters` 仍读旧 | `core/novel/novel-api.ts:1109-1129` |
| F6 | 严重 | `Editor.tsx` 切章「上一章 ref」effect 缺依赖数组 + 切章 effect 缺 `editor/novelId/debouncedSave/chapters` 依赖，存在脏内容写到错章节的窗口期 | `components/novel/Editor.tsx:25-63` |
| F7 | 严重 | `BookImportPage` 三处 `catch (err: any)` 直接读 `err.message` 无空值判断；`pollStatus` 抛错只 `console.error`，UI 永远卡在 "parsing" loading | `components/novel/BookImportPage.tsx:222,311,484` + `230` |
| F8 | 严重 | `BookImportPage` 缓存恢复直接 `delete (data as any)._ts` 不校验 schema，损坏的 sessionStorage 会导致 setState 后渲染期 TypeError 白屏 | `BookImportPage.tsx:75` |
| F9 | 严重 | `NovelHome` 批量删除用 `Promise.all`+计数器，失败被静默吞到 `failCount`，未刷新选中集合，用户可能二次操作触发 404 | `components/novel/NovelHome.tsx:88-117` |
| F10 | 严重 | `useSettingsStore` 把 `apiKey` 明文持久化到 `novelist-settings-storage`（localStorage），与 `@/core/ai/ai-provider-store` 重复存储，存在两份真值 | `core/novel/useSettingsStore.ts:53-108` |
| F11 | 高 | `request()` 入口无 timeout、无 `AbortSignal`，组件 unmount 后请求继续，React Query mutation 不可取消 | `core/novel/novel-api.ts:215-291` |
| F12 | 高 | `AIProjectGenerator.tsx`（1 347 行）单文件混合 SSE 解析、4 步状态机、resume、localStorage、UI 五种职责 | — |
| F13 | 高 | `RelationshipGraph.tsx`（1 246 行）单文件含 dagre 布局、edge 分类、详情面板、保存布局、CharacterDetailContent；第 932 行 `t: any` 显式擦除 i18n 类型 | — |
| F14 | 高 | `forms/` 8 个表单（character/faction/setting/item × new/edit）90% 重复，schema 各自定义，约 1 200 行复制粘贴 | `components/novel/forms/*` |
| F15 | 高 | 8 个组件直接 `fetch(...)` 绕过 `authFetch`：`Organizations.tsx`、`AIProjectGenerator.tsx`、`ChapterAnalysis.tsx`、`ChapterRegenerationModal.tsx`、`reader/ChapterReader.tsx`、`PromptWorkshop.tsx`、`ExpansionPlanEditor.tsx` | — |
| F16 | 高 | `VirtualizedCharacterList.tsx` / `VirtualizedSettingList.tsx` 命名声称虚拟化，实际只是 `ScrollArea + filtered.map`，固定 `h-[200px]`，>200 项卡顿 | `components/novel/sidebar/` |
| F17 | 高 | `EntityManager.tsx:67`、`outline/OutlineView.tsx:77` 用浏览器原生 `window.confirm()` 做删除确认，移动端体验差且可能阻塞 hydration | — |
| F18 | 高 | `Editor.tsx:131-137` "AI is writing..." 浮层 `absolute inset-0 z-50`，但父容器不是 `relative`，可能覆盖整个工作区；同时硬编码英文，绕过 i18n | — |
| F19 | 高 | `NovelWorkspace.tsx`（旧多视图聚合）与 `routes/ProjectWorkspaceLayout.tsx`（新子路由）双导航并存 | — |
| F20 | 高 | mutation 多处 `invalidateQueries({ queryKey: ['novel'] })` 不带 novelId，让所有小说的 novel 查询同时 stale，引发广播式刷新 | `core/novel/queries.ts:229,266,303,341,389,461,495` |
| F21 | 高 | `normalizeNovel` `return { ...raw, ... }` 把 raw 未知字段透传，类型 `as Novel` 强转；zod schema（`schemas.ts`）形同虚设 | `core/novel/novel-api.ts:418-477` |
| F22 | 高 | `useNovelStore` 持完整 `novels/chapters/characters/outlines` 列表，与 React Query cache 完全冗余；persist 又只持 4 个 UI 字段，存在"先空再覆盖"抖动 | `core/novel/useNovelStore.ts:8-129` |
| F23 | 高 | `database.ts` v2 升级把 `novels.id` 由 v1 `++id`（自增数字）改为 `crypto.randomUUID().slice(0, 12)`（字符串），与既有 `++id` 表结构冲突，可能 ConstraintError | `core/novel/database.ts:104-136` |
| F24 | 高 | `app/workspace/novel/` 全树无 `loading.tsx` / `error.tsx` / Skeleton / ErrorBoundary，路由切换无骨架屏，查询失败只能依赖每个组件零散 `text-muted-foreground` 占位 | — |

---

## 四、可优化点路线图

### 4.1 优先级与判定原则

- **P0（立即处理 / 1 周内）**：影响数据正确性、引发故障的根因；harness 边界违规；明显的内存泄漏与并发错乱。
- **P1（短期 / 2-4 周）**：明显的代码重复、巨型文件拆分、统一抽象、性能瓶颈。
- **P2（中期 / 1-2 月）**：架构演进（Postgres 迁移、Alembic、可观测性、A/B 提示词）、测试补全、i18n 与 a11y。

### 4.2 P0：必须优先解决

| 编号 | 任务 | 现状 | 改进 | 收益 |
|------|------|------|------|------|
| P0-1 | **修复 AI 事件总线泄漏 + 多并发流隔离** | 模块级 4 个 listener 永不解绑；`aiStream` 是单例覆盖所有并发流 | 1) 把订阅迁到 React Provider 的 `useEffect` 返回 cleanup；2) `aiStream` 改为按 `requestId` 索引的 `Record<string, AiStreamState>`，事件 payload 已带 requestId；3) 给 `AiEventBus` 增加 `dispose()` 用于 HMR 卸载；4) 给 `requestIdCounter` 加 reset | 彻底消除内存泄漏与 UI 串流，多面板并发可独立显示进度 |
| P0-2 | **修复流式路径 `tool_calls` 静默吞掉** | `generate_text_stream*` 直接 `astream` 后 yield content，模型选 tool 时整段被吞 | 用 `astream_events` v2 收集 `chunk.tool_calls`，命中 tool 调用时切到非流式 `_handle_tool_calls_loop`；或显式禁用 stream + tools 组合并文档化 | 避免一次"看似成功实际为空"的事故 |
| P0-3 | **统一 `datetime` 时区** | `utcnow()` / `now()` / `now(tz=UTC)` 三种混用 | 在 `app/gateway/novel_migrated/core/clock.py` 暴露 `utcnow_aware()`，全量 `grep -r` 替换；加 ruff 自定义规则禁止裸 `datetime.utcnow` / `datetime.now()` | 防止 Postgres 迁移 `TypeError`；lifecycle / recovery 时间比较稳定 |
| P0-4 | **修复 Editor 章节切换竞态** | `previousChapterRef` effect 缺依赖；切章 effect 缺 editor/novelId/chapters 依赖 | 1) 用 `useChapterAutoSave({ chapterId, content })` hook 在 cleanup 里 flush；2) `useEditor` 改用 `key={activeChapterId}` 让 tiptap 重建编辑器实例，彻底放弃手动 `setContent`；3) 切章前 `await flush()` | 消除"写错章节"和"切章空白"两类隐患 |
| P0-5 | **解除 harness → app 反向依赖** | `novel_internal.py` 用 `importlib` 强行 import `app.*`，违反 boundary | 方案 A：把 18 个工具改为只走 HTTP（牺牲性能）；方案 B（推荐）：在 harness 定义 `NovelInternalProtocol` Protocol + 注册函数，由 app 在 startup 时调用 `register_novel_backend(impl)` 注入回调；将 `novel_internal.py` 改为只持有 Protocol 与 registry | 恢复 harness 边界、CI 边界测试可生效、便于工具单测 mock |
| P0-6 | **修复 `_novelCache` / React Query 缓存联动** | 写操作只 `invalidateQueries` 不清 `_novelCache`，30 s 内 `getCharacters/getChapters` 仍读旧 | 删除 `_novelCache`，把 30 s TTL 配到 React Query `staleTime: 30_000`，让 invalidate 行为唯一；同时 `getCharacters/getChapters` 改为独立的 `useQuery` 而非读 novel 子字段 | 消除四级缓存冲突、消除"先空再覆盖"抖动 |
| P0-7 | **修复 `BookImportPage` 错误处理 + 缓存校验** | catch 直接读 `err.message`、缓存恢复不校验 schema | 1) 用 `error instanceof Error ? error.message : String(error)`；2) sessionStorage 缓存改用 `zustand persist + zod schema` 校验；3) `pollStatus` 改 React Query refetchInterval；4) 轮询失败 N 次后切到 'failed' 状态恢复用户操作 | UI 不再卡死 / 不再白屏 |
| P0-8 | **修复 `useOutlineStore.moveNode` 深克隆** | `JSON.parse(JSON.stringify(state.tree))` 丢失 Date / Function / undefined，大纲较大时阻塞 | 改用 `structuredClone()`（Node 17+ / 浏览器原生）或 `immer`；递归操作改成基于 id 的迭代实现，复杂度 O(n²) → O(n) | 大纲千节点仍流畅 |
| P0-9 | **强制鉴权（多用户化阻塞项，本地单用户暂可降级）** | `routers/novel.py` NovelStore 全套无鉴权；`memories.py` 等以 query 参数收 user_id 可绕过 | 1) 删除 `user_id: str | None = None` query 参数，统一 `user_id: str = Depends(get_user_id)`；2) `routers/novel.py` 全部加 `Depends(get_user_id)` + `verify_project_access`；3) 把 `_is_strict_auth_enabled()` 默认改 True，单机模式以 env 显式 opt-out | 关闭越权口子；多用户化解阻塞 |

### 4.3 P1：短期收敛

#### 4.3.1 后端

- **P1-B1 拆分 `novel-api.ts` 1793 行**：按子域拆为 `novels.api.ts` / `chapters.api.ts` / `entities.api.ts` / `careers.api.ts` / `foreshadows.api.ts` / `book-import.api.ts` / `graph.api.ts`，每模块导出函数（无状态），保留兼容 barrel `novelApiService`。
- **P1-B2 抽统一工具装饰器**：写 `@novel_tool("name", entity_type=..., source=...)` 装饰器，把 `check_idempotency / try internal / fallback HTTP / attach_file_truth_meta / DocumentSpec 构造` 收敛到一处；18 个工具 25 行模板降到 3-5 行。
- **P1-B3 模型调用韧性**：用 `tenacity` 包裹 `ainvoke / astream`，对 `httpx.TimeoutException` / `RateLimitError` / `5xx` 重试；区分可重试与不可重试错误；`call_with_json_retry` 只承担 JSON 解析重试。
- **P1-B4 SSE 协议规范**：1) 增加 `id` 字段以便 Last-Event-Id 重连；2) 心跳改 `asyncio.create_task` 固定 15 s 周期；3) `wrapper()` 内的 `GeneratorExit` 转化为对 LLM/db 任务的 cancel；4) 移除硬编码 `Access-Control-Allow-Origin: *`，让 Gateway CORS middleware 接管。
- **P1-B5 错误响应脱敏**：抽 `raise_safe_http(status, public_msg, exc)` 统一记录 `logger.exception(...)` 而对外只返 `public_msg`；admin / changelog / foreshadows / memories / novel_stream 全部替换 `detail=f"...{str(e)}"`。
- **P1-B6 路由前缀统一**：要么所有子 router 加 `/api`，要么由聚合器 `include_router(prefix="/api")` 处统一加；前端调用一致。
- **P1-B7 Pydantic 化老路由**：把 `routers/novel.py` 所有 POST/PUT 改成 `BaseModel + Field(min_length, max_length, ge, le)`、`type` 改 `StrEnum`、路径参数加 `Path(..., regex="^[A-Za-z0-9_-]{1,128}$")`。
- **P1-B8 中间件清理 legacy_store 依赖**：把 `intent_recognition_middleware.py:4074-4133` 的 `_novel_store` 写入逻辑迁到 `novel_query_service.create_novel(...)`，删除"双写"。
- **P1-B9 共享代码抽取**：`_extract_idempotency_key` / `_strip_idempotency_fields` / `_stream_worker` 在 novel.py、chapters.py、novel_stream.py、wizard_stream.py 重复出现，拉到 `novel_migrated/api/_shared.py`。
- **P1-B10 限流外部化**：`_STREAM_REQUEST_WINDOWS` / `_ANALYSIS_TASKS` / `_ANALYSIS_RESULTS` 改用 SQLite 临时表或 Redis；不依赖 in-process 字典。

#### 4.3.2 前端

- **P1-F1 单一真相收敛**：服务器数据全部由 React Query 持有，删除 `useNovelStore.{novels,chapters,characters,outlines}` 与 `sync-hooks.ts`；`useNovelStore` 只保留 UI 状态（`currentNovelTitle / activeChapterId / viewMode / isImmersive`）；Dexie 仅作为离线快照，由 `novel-domain-service` 在 mutation `onSuccess` 写入。
- **P1-F2 拆 `AIProjectGenerator.tsx`**：抽 `useWizardSseStream` / `useWizardResume` hook；4 步串行流程拆成 `WizardStepRunner + steps[]` 配置；UI 与 logic 分离到 `AIProjectGenerator.view.tsx`。目标：单文件 < 300 行。
- **P1-F3 抽 `useSseStream<T>()` + `<StreamingProgress>`**：5+ 处 SSE 解析合并；UI 端只关心 `{ status, progress, data, cancel }`；`SSEProgressModal / SSELoadingOverlay / Editor 内联浮层 / LoadingOverlay` 合并为 `<StreamingProgress variant="modal|inline|toast" cancelable />`。
- **P1-F4 消除 `forms/` 双胞胎**：合并为 `<XxxForm mode="create"|"edit" initial?: Xxx />`，`forms/EntityForm.tsx` 用 `entityFieldsConfig[entityType]` 渲染，配合 `useEntityMutation(entityType, mode)`；schema 来自 `core/novel/schemas.ts`。
- **P1-F5 统一 API 客户端层**：抽 `createApiClient({ baseURL, timeout, fetcher: authFetch })`；所有方法接 `signal`，非流默认接管 `AbortController` 挂到 React Query mutationKey；用 `utils/errorhandler.ts` 的 `handleError` 中心化错误归一；retry/熔断对接 `globalCircuitBreaker` + `globalRetryManager`。
- **P1-F6 拆 `RelationshipGraph.tsx`**：拆 `useGraphData / useGraphLayout / GraphCanvas / NodeDetailPanel / CareerOverlay`；`t: any` 改正确类型。
- **P1-F7 替换"假虚拟化"列表**：`VirtualizedCharacterList` / `VirtualizedSettingList` / `AnnotationThreadPanel` 改用 `@tanstack/react-virtual`（已在 BookImportPage 使用）；search 加 `useDebouncedValue(150ms)` 后再 filter。
- **P1-F8 统一 fetch 入口**：8 个直连组件迁到 `core/api/fetcher.authFetch`；ad-hoc 接口归并到 `novelApiService`；`useEffect+fetch+useState` 三件套换 `useQuery`。
- **P1-F9 补 App Router 边界文件**：在 `app/workspace/novel/` / `[novelId]/` / `[novelId]/chapters/` 各加 `loading.tsx`（骨架屏）、`error.tsx`（统一 toast + 重试）；编辑器、reader、relationships/graph 路由 `<Suspense>` 兜底。
- **P1-F10 解决双导航并存**：保留 `ProjectWorkspaceLayout`（路由真源），删除 `NovelWorkspace.tsx` 与 `useNovelStore.viewMode`；`viewMode` 集中视图迁到 chapters 路由内或独立 page。
- **P1-F11 selector 性能**：强制单字段 selector：`useNovelStore(s => s.setChapters)`；提供 `useNovelStoreShallow(['x','y'])` 包装；`useSettingsStore` 移除 `apiKey/llmProvider/promptTemplates`，统一交给 `@/core/ai` store；`useStyleStore` 增加 persist。
- **P1-F12 类型生成**：后端生成 OpenAPI，前端用 `openapi-typescript` 或 `orval` 生成 TS 接口；边界 normalize 用 zod `.transform()` 同时完成 snake_case → camelCase 与默认值；删除手写 `interface` 重名定义；mutation `invalidateQueries` 必须带 novelId。

### 4.4 P2：中期演进

- **P2-1 提示词模板全量外置 + A/B**：把 `career_service.get_career_generation_prompt` / `prompt_service.build_novel_cover_prompt` 等内联 f-string 全量迁入 `prompt_templates.yaml`；`prompt_template` 表加 `version`、`activated_at` 字段，支持 A/B 切换 + 回滚。前端 `ai-service.ts` 的硬编码中文 prompt 也改为从后端拉取。
- **P2-2 存储升级路径**：短期保留 SQLite，但用 alembic 管理 schema（替换 `database.py:140 _ensure_version_columns` 拼字符串）；中期切到 Postgres + 文件存对象存储；引入 outbox 模式让 DB 与文件由消息驱动同步，解决双写一致性。
- **P2-3 幂等键持久化升级**：从单键单文件改为 SQLite 表 `idempotency_keys(hash PRIMARY KEY, tool, expires_at)` + 索引，多 worker 共享；TTL 失效自动清理；指标接 Prometheus / OpenTelemetry。
- **P2-4 service 复杂度治理**：`book_import_service.py`（2 725 行）拆 `book_import_pipeline.py`（编排）/ `book_import_persist.py`（落库）/ `book_import_wizard.py`（向导）；`foreshadow_service.py`（1 699 行）拆 `crud / sync / matching / context_builder`；`ai_service.py`（1 119 行）拆 `model_factory_cache / chat / streaming / tool_calls`。
- **P2-5 巨型组件拆分**：`ChapterAnalysis.tsx`（1 032 行）按 tab 拆 `OverallTab/HooksTab/ForeshadowsTab/MemoryTab`；同样思路适用于 `CareersView.tsx`(507) / `Organizations.tsx`(508) / `reader/QualityReportPanel.tsx`(583) / `reader/AnnotationThreadPanel.tsx`(521)。
- **P2-6 测试补全**：后端 `tests/` 仅 1 个测试文件，需补：`test_novel_idempotency.py`（重启加载、TTL、并发）、`test_workspace_document_service.py`（路径越界、manifest 损坏恢复）、`test_orchestration_service.py`（lifecycle replay、token 超时）、`test_optimistic_lock.py`（version 冲突重试）、`test_ai_service_cache.py`（key 哈希稳定性）、`test_prompt_service.py`（YAML 热加载）。前端补 `useAiPanelStore` 的并发流测试、`useSseStream` 的 abort/reconnect 测试。
- **P2-7 可观测性补全**：`observability.ts` 当前只 7 个事件，AI / 导入 / 熔断 / 重试均未上报；在 `globalAiService` / SSE 客户端关键节点 emit `ai_generate_start/complete/error`、`stream_chunk_count`、`circuit_state_change`；提供 `setObserver(fn)` 注入 telemetry。后端补 `_call_stats` / `model_cache_stats` / `idempotency` 的 Prometheus 指标。
- **P2-8 i18n 全量覆盖**：18 个文件包含中文硬编码，按模块（careers/foreshadows/book-import/inspiration/quality）补全到 `core/i18n/locales/`；用 ESLint 规则禁止新代码引入中文字面量。
- **P2-9 a11y 与设计 token**：tab 列表加 `role="tablist"/tab"`、`aria-selected`、`focus-visible:ring`；`window.confirm` 替换为 `<Dialog>`；TTS 进度补 `aria-live`；`AnnotatedText.tsx` / `RelationshipGraph.tsx` / `reader/ChapterReader.tsx` 硬编码颜色迁到 `tailwind.config` semantic color，支持暗色模式。
- **P2-10 配置中心统一**：`MODEL_CACHE_MAX_SIZE=64`、`max_iterations=5`、`tail_chapter_count` 等散落硬编码汇集到 `config.yaml.novel` 节，pydantic Settings 注入。

### 4.5 优先级建议

如果团队资源有限，建议第一周聚焦：**P0-1（事件泄漏） / P0-2（tool_calls 吞掉） / P0-4（Editor 竞态） / P0-6（缓存联动） / P0-7（导入页错误）**。这五个 P0 风险最直接面向用户体验，且代码改动量都在百行以内，性价比最高。**P0-3（datetime） / P0-5（边界） / P0-8（深克隆） / P0-9（鉴权）** 在第二周收敛即可。

---

## 五、典型代码片段

> 摘录最具代表性的问题代码与修复方向。完整路径已标出，可直接定位。

### 5.1 AI 事件总线监听器泄漏（F1）

```ts
// frontend/src/core/novel/useAiPanelStore.ts:77-99
aiEventBus.on('stream_start', () => {
  useAiPanelStore.getState().startStreaming();
});
aiEventBus.on('stream_chunk', (event) => {
  const chunk = event.payload.chunk as string;
  if (chunk) useAiPanelStore.getState().addChunk(chunk);
});
aiEventBus.on('stream_complete', () => useAiPanelStore.getState().stopStreaming());
aiEventBus.on('stream_error',   (event) => useAiPanelStore.getState().setError(...));
aiEventBus.on('stream_abort',   () => useAiPanelStore.getState().stopStreaming());
```

**问题**：模块级注册 listener 永不解绑；HMR / RSC 重新执行时反复入栈；`Set` 持续膨胀；多并发流共用同一 store → 内存泄漏 + UI 数据串流。

**修复方向**：

```tsx
// frontend/src/components/novel/NovelAiBridge.tsx (新建)
export function NovelAiBridge() {
  const startStreaming = useAiPanelStore(s => s.startStreaming);
  const addChunk = useAiPanelStore(s => s.addChunk);
  // ... 其他 selector
  useEffect(() => {
    const offs = [
      aiEventBus.on('stream_start', (e) => startStreaming(e.requestId)),
      aiEventBus.on('stream_chunk', (e) => addChunk(e.requestId, e.payload.chunk as string)),
      // ...
    ];
    return () => { offs.forEach(off => off()); };
  }, [startStreaming, addChunk]);
  return null;
}
// 同时把 aiStream 改成 Record<requestId, AiStreamState>
```

### 5.2 流式路径 `tool_calls` 静默吞掉（B2）

```python
# app/gateway/novel_migrated/services/ai_service.py:826-886
async def generate_text_stream(self, prompt, ..., auto_mcp=True, ...):
    tools = await self._prepare_mcp_tools(auto_mcp=auto_mcp)
    if tools:
        try:
            llm = llm.bind_tools(tools)
        except Exception as exc:
            logger.warning("当前模型不支持 bind_tools，忽略 MCP 工具: %s", exc)
    messages = self._build_messages(prompt, system_prompt)
    async for chunk in llm.astream(messages):
        ...
        if text:
            yield str(text)
```

**问题**：bind 了工具但完全没处理 `chunk.tool_calls`，模型选择调用工具时 content 段为空，调用方拿到空字符串、不知错。

**修复方向**：用 `astream_events` v2 收集 `tool_call_chunks`，命中工具调用时切换到非流式 `_handle_tool_calls_loop`；或在工具数 > 0 时显式 disable streaming 并文档化"stream + tools 不同时支持"。

### 5.3 Editor 章节切换竞态（F6）

```tsx
// frontend/src/components/novel/Editor.tsx:25-27
useEffect(() => {
  previousChapterRef.current = { id: activeChapterId || '', content: dirtyContent };
});
```

**问题**：缺依赖数组，每渲染都覆盖 ref；切章 effect 拿到的是"最近一次 render"而非"切章前一帧"，配合 dirtyContent 异步可能写错章节。

**修复方向**：

```tsx
// 方案：放弃手写 ref，改用 tiptap 的 key 重建
const editor = useEditor({
  extensions, content: chapter?.content || '',
}, [activeChapterId]);  // key 触发实例重建

// 或用 hook 内部 cleanup flush
function useChapterAutoSave({ chapterId, content, save }) {
  useEffect(() => {
    return () => { void save(chapterId, content); };
  }, [chapterId]);  // 只在 chapterId 变化时 flush 上一章
}
```

### 5.4 harness 反向依赖 app（B1）

```python
# packages/harness/deerflow/tools/builtins/novel_internal.py:25-30
def _check_internal_modules() -> bool:
    try:
        mod = importlib.import_module("app.gateway.novel_migrated.core.database")
        return hasattr(mod, "AsyncSessionLocal")
    except Exception:
        return False
```

**问题**：harness 包反向 `importlib` app 层模块，违背 `tests/test_harness_boundary.py` 边界测试与 `backend/CLAUDE.md` 文档约束；harness 单独发布即崩。

**修复方向（推荐）**：

```python
# packages/harness/deerflow/tools/builtins/novel_internal.py 改写后
from typing import Protocol, Optional

class NovelInternalProtocol(Protocol):
    async def get_db_session(self) -> "AsyncSession": ...
    async def get_ai_service(self, user_id, module_id) -> Any: ...
    def resolve_user_id(self, raw: Optional[str]) -> str: ...

_impl: Optional[NovelInternalProtocol] = None

def register_novel_backend(impl: NovelInternalProtocol) -> None:
    global _impl
    _impl = impl

# app/gateway/app.py startup 时调用
# from deerflow.tools.builtins.novel_internal import register_novel_backend
# register_novel_backend(MyAppNovelBackend())
```

### 5.5 越权 user_id 查询参数（B7）

```python
# app/gateway/novel_migrated/api/memories.py:528-551
@router.post("/projects/{project_id}/search")
async def search_memories(
    project_id: str, query: str,
    request: Request = None, db: AsyncSession = Depends(get_db),
    user_id: str | None = None,            # ← 客户端可控
):
    effective_user_id = user_id if user_id else get_user_id(request) if request else "local_single_user"
    await verify_project_access(project_id, effective_user_id, db)
```

**问题**：`?user_id=victim` 即可绕过 token 把 `effective_user_id` 替换为受害者；同样反模式在 `careers.py:281`、`chapters.py:510`、`novel_stream.py:1301`、`memories.py:95` 重复出现。

**修复方向**：

```python
@router.post("/projects/{project_id}/search")
async def search_memories(
    project_id: str, query: str,
    user_id: str = Depends(get_user_id),  # ← 仅从 token / state 取
    db: AsyncSession = Depends(get_db),
):
    await verify_project_access(project_id, user_id, db)
    ...
```

### 5.6 datetime 时区混用（B4）

```python
# foreshadow_service.py:1450
fs.planted_at = datetime.now()  # naive

# foreshadow_service.py:1533
foreshadow.created_at = datetime.utcnow()  # naive

# lifecycle_service.py:117
return datetime.now(tz=UTC)  # aware
```

**问题**：SQLite 暂不报错，迁 Postgres 必炸；lifecycle 校验 `now - planted_at > TTL` 在不同 service 之间结果不可预期。

**修复方向**：

```python
# 新建 app/gateway/novel_migrated/core/clock.py
from datetime import UTC, datetime
def utcnow_aware() -> datetime:
    return datetime.now(tz=UTC)

# 全量 grep -rn "datetime.utcnow\|datetime.now()" 替换
# 加 ruff 自定义规则禁止裸 datetime.utcnow 与 datetime.now()
```

### 5.7 _novelCache 与 React Query 缓存不联动（F5）

```ts
// frontend/src/core/novel/novel-api.ts:1109-1129
private _novelCache = createNovelCache<Novel>();
async getNovelCached(novelId: string): Promise<Novel | null> {
  return getCachedOrLoadNovel(this._novelCache, novelId, () => this.getNovelByIdOrTitle(novelId));
}
async getCharacters(novelId: string): Promise<Character[]> {
  const novel = await this.getNovelCached(novelId);
  return novel?.characters ?? [];
}
```

**问题**：`updateNovel/createChapter/updateChapter` 等写路径不会触发 `_novelCache.delete(novelId)`；`getCharacters/getChapters` 都依赖该缓存，更新后 30 秒内仍读旧；React Query `['novel', id]` 与该缓存重复。

**修复方向**：删除 `_novelCache`，把 30 s TTL 配到 React Query `staleTime: 30_000`；`getCharacters/getChapters` 改为独立的 `useCharactersQuery(novelId)` / `useChaptersQuery(novelId)`，让 invalidate 行为唯一。

### 5.8 后端 NovelStore 老路由无鉴权（B6 / B13）

```python
# backend/app/gateway/routers/novel.py:974-979
@router.post("/novels")
@router.post("/novel/novels", deprecated=True)
async def create_novel(request: Request):
    """Create a new novel project."""
    data = await request.json()
    return await _novel_store.create_novel(data)
```

**问题**：路由无 `Depends(get_user_id)`、无身份校验；body 直接收 dict，任何字段（包括 `id`、`syncStatus`、`metadata.userId`）都能被注入并写入 `data/novel_store.json`；`delete_novel` 还会回退到 modern store 按 `project_id` 删除其他用户真实数据。

**修复方向**：

```python
class NovelCreateRequest(BaseModel):
    title: str = Field(min_length=1, max_length=200)
    description: str = Field(default="", max_length=2000)
    genre: str | None = Field(default=None, max_length=100)
    # ...

@router.post("/novels")
async def create_novel(
    payload: NovelCreateRequest,
    user_id: str = Depends(get_user_id),
):
    return await _novel_store.create_novel({**payload.model_dump(), "user_id": user_id})
```

最佳方案是把 `routers/novel.py` 整体迁到 `novel_migrated`，在 routes 层用 `NOVEL_LEGACY_ROUTES_ENABLED` env 开关控制（参考 admin 路由），逐步下线。

### 5.9 SSE 解析重复 5 份（前端 F12 / 后端 P1-B2）

```tsx
// frontend/src/components/novel/AIProjectGenerator.tsx:91-200 (110 行)
async function streamPost(url, body, callbacks, modelConfig) {
  // fetch -> reader.read -> 自实现 SSE 块解析 -> dispatch progress/result/error/complete/done
}
// 同样的逻辑在 ChapterRegenerationModal.tsx:48-130 / InspirationMode 内 / AiChatView 内 / Editor / GeneratePanel 各重写
```

**修复方向**：

```ts
// frontend/src/core/novel/sse-client.ts (新建)
export function createSseStream<T>(opts: {
  url: string; body?: unknown; signal?: AbortSignal;
  onEvent: (event: { type: string; payload: T }) => void;
  onComplete?: (final: T) => void;
}): () => void { /* 内部用 utils/stream.ts:parseSseStream 实现 */ }

// React 侧
export function useSseStream<T>(opts) {
  const [state, setState] = useState<{ progress: number; data?: T; error?: string }>({...});
  const cancelRef = useRef<() => void>();
  const start = useCallback((body) => {
    cancelRef.current?.();
    cancelRef.current = createSseStream({ ...opts, onEvent: ev => setState(s => reduce(s, ev)) });
  }, [opts]);
  useEffect(() => () => cancelRef.current?.(), []);
  return { state, start, cancel: () => cancelRef.current?.() };
}
```

后端同步：抽 `@novel_tool` 装饰器，把 18 个工具的 `check_idempotency / try internal / fallback HTTP / attach_file_truth_meta` 收敛到一处。

---

## 六、与历史报告的关系

| 历史报告 | 主要结论 | 本报告关系 |
|---------|---------|-----------|
| `CODE_REVIEW_REPORT.md`（2026-04-24） | 中间件膨胀、双写崩溃、prompt_service 内联模板 | 双写已通过新增 `async_session_factory` 修复；prompt_service 已外置 YAML，但仍有部分 service 内联 f-string；本报告补充新发现的 AI 事件泄漏、Editor 竞态、缓存联动 |
| `Novel_Creation_Process_Assessment_Report.md` | 创作流程 19 环节，覆盖率 73.7% | 本报告聚焦"工程质量"而非"功能完成度"，互补 |
| `novel-tool-gap-analysis.md` | 14 个对话流工具已注册（73.7% 覆盖率） | 本报告补充工具层的"代码重复"与"边界违规"问题 |
| `小说提示词模板系统排查报告.md` / `移植完成度检查报告.md` | YAML 外置 + 30 s TTL 缓存 + DB 用户自定义已就绪 | 本报告确认 YAML 已搭好，但部分 service 仍硬编码（P2-1） |
| `主项目写作流程与小说创作深度联动打通方案.md` | 主项目对话流与小说后端打通方案 | 本报告未涉及，建议后续单独审查 |

---

## 七、附录

### 7.1 严重问题汇总

后端：B1（边界）、B2（tool_calls 吞）、B3（fallback 双落库）、B4（datetime 混用）、B5（幂等键单文件爆量）、B6（老路由无鉴权）、B7（user_id 越权）、B8（双写）。

前端：F1（事件泄漏）、F2（流串扰）、F3（双写）、F4（深克隆崩）、F5（缓存陈旧）、F6（Editor 竞态）、F7（导入卡死）、F8（缓存白屏）、F9（删除静默失败）、F10（apiKey 明文）。

### 7.2 巨型文件清单（按行数倒序）

后端：

```
2725  services/book_import_service.py
1793  frontend/src/core/novel/novel-api.ts
1699  services/foreshadow_service.py
1552  api/novel_stream.py
1353  routers/novel.py
1296  api/settings.py
1231  services/memory_service.py
1207  resources/prompt_templates.yaml
1119  services/ai_service.py
1056  api/careers.py
 942  api/prompt_workshop.py
 936  services/ai_settings_service.py
 934  api/chapters.py
 872  tools/builtins/novel_creation_tools.py
 837  tools/builtins/novel_tools.py
 776  services/orchestration_service.py
 764  api/memories.py
 740  tools/builtins/novel_analysis_tools.py
 730  frontend/src/core/novel/database.ts
 705  services/workspace_document_service.py
```

前端：

```
1793  core/novel/novel-api.ts
1347  components/novel/AIProjectGenerator.tsx
1246  components/novel/RelationshipGraph.tsx
1032  components/novel/ChapterAnalysis.tsx
 847  components/novel/BookImportPage.tsx
 732  core/novel/queries.ts
 730  core/novel/database.ts
 642  components/novel/CharacterCareerCard.tsx
 642  core/novel/phase2-status.ts
 593  components/novel/InspirationMode.tsx
 508  components/novel/Organizations.tsx
 507  components/novel/CareersView.tsx
 465  components/novel/EditorToolbar.tsx
 459  components/novel/ChapterRegenerationModal.tsx
 455  core/novel/schemas.ts
 445  core/novel/novel-domain-service.ts
 436  components/novel/ForeshadowsView.tsx
```

### 7.3 关键路径速查

- 后端 API 入口：`backend/app/gateway/app.py:118-120 → CORE_ROUTER_MODULES`（注册 `routers.novel` + `routers.novel_migrated`）
- 后端 novel_migrated 聚合：`backend/app/gateway/routers/novel_migrated.py:16-46 → _OPTIONAL_ROUTER_MODULES`（27 个子模块）
- 后端鉴权工具：`backend/app/gateway/novel_migrated/api/common.py:46-110`（`require_authenticated_user` / `verify_project_access`）+ `core/user_context.py`
- 后端数据库：`backend/app/gateway/novel_migrated/core/database.py`（SQLite + WAL，路径 `.deer-flow/novel_migrated.db`）
- 后端 AI 服务：`backend/app/gateway/novel_migrated/services/ai_service.py`（模型缓存 + tool_calls loop）
- 后端提示词：`backend/app/gateway/novel_migrated/services/prompt_service.py` + `resources/prompt_templates.yaml`
- 后端 AI 工具：`backend/packages/harness/deerflow/tools/builtins/novel_*.py`
- 前端 AI 桥接：`frontend/src/core/novel/ai-service.ts → @/core/ai/global-ai-service`
- 前端事件总线：`frontend/src/core/novel/ai-event-bus.ts`（单例 `aiEventBus`）
- 前端状态管理：`frontend/src/core/novel/use{Novel,AiPanel,Outline,Editor,Settings,Style,Modal}Store.ts`
- 前端 API 客户端：`frontend/src/core/novel/novel-api.ts`（单类 1 793 行）
- 前端路由：`frontend/src/app/workspace/novel/[novelId]/{layout.tsx,page.tsx,...}`

---

**审查人**：4 路并行代理 + 主代理汇总 | **报告版本**：v1.0 | **下一次审查建议**：完成 P0 后回看是否新增问题。

