/**
 * About Miaowu OS markdown content. Inlined to avoid raw-loader dependency
 * (Turbopack cannot resolve raw-loader for .md imports).
 */
export const aboutMarkdown = `# 关于 Miaowu OS

> 面向小说作者的 AI 创作工作台

**Miaowu OS** 是围绕长篇小说创作流程构建的本地优先工作台。它把作品管理、作者控制台、场景计划、智能续写、证据化审校、可控修订、版本回滚、图片生成、TTS 有声书和 NewAPI 多分组模型接入放在同一个作者闭环中，而不是只提供零散的 AI 调用按钮。

---

## 快速开始

1. 登录后进入工作区。左侧导航分为“创作工作台”和“通用智能体”。
2. 写小说时先进入“小说工作室”，创建或选择一本作品。
3. 进入作品后，先补世界设定、角色、大纲，再到章节管理写正文。
4. 需要 AI 参与时，优先使用项目内“作者控制台”，它会围绕当前作品上下文生成场景、审校问题和候选版本。
5. 需要导入已有 TXT 时，使用“拆书导入”；需要配图时，使用“图片生成”；需要朗读或有声书时，在章节阅读器里使用 TTS 播放器。
6. 第一次使用模型前，进入“设置 -> AI 供应商”配置模型，或点击“同步 NewAPI 分组/密钥”同步 NewAPI 可用分组。

---

## 工作区导航

* **小说工作室**：作品列表和创作入口。可创建小说、进入项目、管理章节和结构化资料。
* **作者控制台**：面向具体作品的纵向创作闭环。当前全局入口会先回到小说工作室；需要先选择作品，再在项目内左侧进入“作者控制台”。
* **灵感模式**：前期脑暴入口，用于快速生成创意选项、细化设定和扩展素材；它不会自动写入正式章节。
* **拆书导入**：上传 TXT，预览章节拆分结果，确认后导入为新作品或写入当前作品。
* **图片生成**：通过提示词生成图片，支持尺寸、比例、质量、数量、历史任务、预览、下载和复制 URL。
* **对话**：通用智能体聊天入口，适合研究、问答、文件和产物协作。
* **智能体**：查看、创建和进入自定义 Agent。
* **设置和更多**：账号、外观、AI 供应商、草稿、记忆、工具、技能、通知、管理后台和关于页。

---

## 小说工作室怎么用

1. 在左侧点击“小说工作室”。
2. 点击创建作品，填写标题、简介和基础信息。
3. 进入作品后，项目内左侧会出现三组功能：
   * **核心内容**：章节管理、作者控制台、世界设定、角色、大纲。
   * **关系与组织**：关系概览、关系图谱、组织、职业体系。
   * **增强能力**：一致性报告、伏笔、导入素材、写作风格、Prompt 工坊、设置。
4. 推荐顺序是：世界设定 -> 角色 -> 大纲 -> 章节正文 -> 作者控制台 -> 一致性报告/伏笔 -> 修订和回滚。

结构化资料会影响后续 AI 上下文。世界设定、角色状态、章节正文、伏笔和作品文档属于作品事实库；用户长期偏好属于主记忆，两者不要混用。

---

## 章节、编辑和阅读

* **章节管理**用于创建章节、编辑正文、查看章节列表和进入单章。
* **章节编辑器**适合人工写作和局部修改；AI 生成、续写、重生成和分析会基于当前作品上下文工作。
* **章节分析**用于检查当前章节的结构、问题和可改进点。
* **阅读模式**用于沉浸阅读、切换章节、调整显示效果。
* **阅读工作台**包含章节阅读、版本对比、批注、创作建议和质量相关面板。

如果生成失败，先查看页面提示和任务状态，再重试失败步骤。不要把“页面能打开”等同于“模型调用已成功”。

---

## 作者控制台

作者控制台用于把“让 AI 写一段”变成可确认的创作流程：

1. 先在小说工作室选择一本作品。
2. 在项目内左侧点击“作者控制台”。
3. 选择当前章节，查看上下文预览。
4. 生成场景计划，确认场景目标、冲突和角色状态。
5. 生成正文候选或修订候选。
6. 使用审校功能查看问题、建议和章节 critique。
7. 对比版本差异，确认后写回；不满意可以拒绝或回滚。

它适合续写、扩写、压缩、对白、描写、换 POV、润色等任务。正式写回前应先看 diff，不要把 AI 候选当成自动定稿。

---

## 设定、关系和质量

* **世界设定**：记录地点、规则、势力背景和世界观信息。
* **角色**：管理人物档案、状态、关系和当前阶段设定。
* **大纲**：维护卷、章节、剧情节点和发展方向。
* **关系概览/关系图谱**：查看角色、组织和关系结构。
* **组织/职业体系**：维护势力、职业、等级和身份体系。
* **伏笔**：记录埋伏笔、回收、放弃和关联章节。
* **一致性报告**：按需检查章节、角色、伏笔、时间线和结构问题。它不是每个页面都实时自动重算。

---

## 拆书导入

拆书导入适合把已有 TXT 变成可继续创作的项目。

1. 从左侧“拆书导入”进入，或在某本作品内进入“导入素材”。
2. 上传 TXT。建议章节标题使用“第 X 章”这类清晰格式。
3. 选择解析范围。末尾章节反向生成适合保留当前风格，整本导入适合完整整理。
4. 预览章节列表，必要时修改章节标题和内容。
5. 点击确认导入，等待 SSE 进度完成。
6. 导入失败时，优先重试失败步骤，不要重复执行已完成步骤。

---

## 写作风格和 Prompt 工坊

* **写作风格**保存作品级风格偏好，例如叙述口吻、节奏、描写密度和风格标签，会影响后续生成。
* **Prompt 工坊**用于查看、导入、下载、提交和管理提示词素材。
* **提示词模板**是具体任务使用的模板，例如大纲生成、续写、润色、扩写、聊天和信息抽取。

写作风格是作品偏好；Prompt 模板是生成任务的指令结构。两者都能影响输出，但用途不同。

---

## 图片生成

在左侧点击“图片生成”：

1. 输入 prompt。
2. 选择模型、尺寸、比例、质量和数量。数量范围通常是 1 到 10。
3. 尺寸和比例不要同时指定；二选一即可。
4. 提交后在历史任务里查看状态、错误、预览图、下载和复制 URL。
5. 模型留空时走后端默认配置。

图片生成依赖后端 provider、存储和权限配置。页面可打开不代表图片 provider 已经可用。

---

## TTS 和有声书

章节阅读器里提供 TTS 播放器，分为两类：

* **设备朗读**：使用浏览器或系统自带声音，不生成服务端音频，适合快速预听。
* **AI 有声书**：由服务端生成章节音频，可复用、重生成、下载，也可使用旁白计划和多角色声音。

使用建议：

1. 先打开某一章的阅读页面。
2. 在 TTS 设置里选择设备朗读或服务端生成。
3. 服务端生成失败时，检查 provider、voice、章节内容和后端 TTS 配置。
4. 需要导出整章 MP3 时，可使用浏览器本机 FFmpeg.wasm 合并；长章节会更耗内存和时间。

普通 \`OPENAI_BASE_URL\` 不等于 OpenAI TTS 一定可用。OpenAI TTS 需要显式 TTS 配置或用户模型路由；没有显式配置时，系统可能使用本地 \`moss-local\` 能力。

---

## AI 供应商和 NewAPI 分组同步

进入“设置 -> AI 供应商”可以管理模型来源和功能级模型路由。通用聊天的主模型通常在聊天输入框顶部选择；AI 供应商页主要配置记忆、灵感、小说工作流、图片、TTS 等系统能力模型。

### 同步 NewAPI 分组

NewAPI 的模型可见性与分组绑定。一个账号可能同时有 \`default\`、\`vip\`、\`svip\` 等分组，不同分组可用模型不同。Miaowu OS 会把每个同步成功的分组保存为一个独立的后端托管 provider。

推荐步骤：

1. 打开“设置 -> AI 供应商”。
2. 点击“同步 NewAPI 分组/密钥”。
3. 如果提示缺少同步令牌，点击“重新登录 NewAPI”，在新标签页完成登录后回到原页面。
4. 点击“重新发现分组”。
5. 勾选发现到的分组。
6. 如果 NewAPI 目录不完整，在“手动输入分组名”里输入分组名；多个分组可用逗号、空格或换行分隔，例如 \`default, vip, svip\`。
7. 点击“同步选中分组”。
8. 同步后回到 AI 供应商列表，确认出现多个 NewAPI 分组 provider，并选择有模型的 provider 作为默认或功能路由目标。

状态说明：

* **已同步**：该分组已创建或复用后端密钥，并成功获得模型列表。
* **无模型**：分组存在，但 NewAPI 没有返回可用模型；不建议设为默认。
* **失败**：分组 bootstrap 或模型同步失败；可重新登录 NewAPI 后再同步，或检查 NewAPI 后台权限。
* **未同步**：系统知道这个分组，但本地还没有保存为 provider。

安全边界：

* 浏览器不会显示或保存 NewAPI API Key。
* 后端为每个分组创建或复用密钥，并只向前端返回“已有服务端密钥”等脱敏状态。
* 手动输入分组名不是绕过权限；当前 NewAPI 用户没有权限或后端没有有效同步令牌时，同步仍会失败。
* 同步成功的事实源是 \`/api/user/ai-settings\` 返回的 provider 列表，不是 localStorage。

---

## 账号、权益和管理后台

* **账号页**展示 NewAPI 账号信息、余额/额度快照、Miaowu 会员权益、服务端空间、本地缓存、密码和退出登录。
* **NewAPI 额度**是上游 AI 调用额度。
* **Miaowu 会员权益**控制项目空间、项目数、Agent runs、并发、优先队列等产品权限。
* **管理后台**仅管理员可见，用于系统空间设置、用户配额、存储重算、审计日志和权益刷新。

不要把 NewAPI 余额和 Miaowu 会员权益混成同一个额度概念。

---

## 草稿、记忆、工具和技能

* **草稿**保存本地未完成输入和临时内容。草稿不是云端事实源。
* **记忆**保存用户长期偏好、工作背景和常用信息。小说剧情事实应放在作品资料和 Novel RAG 中。
* **工具**展示 MCP 工具配置和可用状态。
* **技能**用于通用智能体扩展，不等同于某一本小说的剧情设定。
* **通知**用于配置工作区提醒和状态反馈。

---

## 本地开发和部署边界

* Windows 本地开发场景中，后端基址固定为 \`http://127.0.0.1:8551\`。
* 前端默认使用 4560 或当前 Windows 可用端口。
* 不要把 8001 写成 Miaowu OS local-dev 默认后端地址。
* 浏览器访问和服务到服务调用可能走不同网络路径；排查时要分别看前端请求地址、后端 gateway、NewAPI \`/v1/models\` 和真实 provider 响应。
* 页面能登录不代表 API 调用成功。模型列表、图片生成、TTS、有声书和 NewAPI 分组同步都需要对应后端接口和 provider 可用。

---

## 开源基础

Miaowu OS 基于开源项目 **DeerFlow** 改造。DeerFlow 采用 MIT License，允许在遵守许可条款的前提下使用、修改和再分发。

原版官方仓库：[github.com/bytedance/deer-flow](https://github.com/bytedance/deer-flow)

---

## 致谢

感谢 DeerFlow、LangGraph、LangChain、Next.js、shadcn/ui 以及相关开源社区提供的基础能力。Miaowu OS 在此基础上继续面向中文长篇小说创作、作者控制台和本地部署体验做产品化改造。
`;
